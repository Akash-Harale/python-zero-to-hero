# JS

# let user = { name: "Akash", age: 25 }
# user.name
# user["age"]
# user.city = "Delhi"
# Object.keys(user)
# Object.values(user)
# "name" in user

# Python
user = { "name": "Akash", "age": 25 }
user["name"]           # no dot notation for dicts
user["age"]
user["city"] = "Delhi"
user.keys()
user.values()
"name" in user         # same!

print("name" in user) # True -> return the Boolean value
print("city" in user) # True
print(user["name"]) # Akash
print(user.keys())
print(user.values())