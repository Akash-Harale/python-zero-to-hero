# JS
# function add(a, b) {
#   return a + b
# }

# // arrow function
# const multiply = (a, b) => a * b

# // default params
# function greet(name = "World") {
#   return `Hello ${name}`
# }

# Python — indent based, no braces
def add(a, b):
    return a + b

# lambda = arrow function
multiply = lambda a, b: a * b

# default params — same concept
def greet(name="World"):
    return f"Hello {name}"


